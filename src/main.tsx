import React from 'react';
import ReactDOM from 'react-dom/client';
import { TestComponent } from 'react-carousel-image-optimized';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TestComponent />
  </React.StrictMode>
);
